# MusicWeb Nyx-Step — Python Requirements
# Pinned to known-good major versions (installed versions as of 2026-06-10)

# Web framework
fastapi>=0.135,<1.0
uvicorn[standard]>=0.41,<1.0
jinja2>=3.1,<4.0
python-multipart>=0.0.22
sse-starlette>=3.4,<4.0
requests>=2.33,<3.0
httpx>=0.28,<1.0
aiofiles>=23.0

# Video generation (frame extraction in core/video_orchestrator.py)
opencv-python-headless>=4.8,<5.0

# Audio analysis (BPM, key, LUFS, spectral, LRC, quality score)
librosa>=0.11.0,<1.0
soundfile>=0.13.0
pyloudnorm>=0.2.0
mutagen>=1.47.0

# Speech-to-text (lyrics transcription + Analyze tab)
faster-whisper>=1.0.0,<2.0

# Stem separation (Demucs tab)
demucs>=4.0.0,<5.0

# MIDI extraction (MIDI tab — melody mode)
pretty-midi>=0.2.10
mido>=1.3.0,<2.0

# MIDI extraction (MIDI tab — polyphonic piano mode, requires CUDA GPU)
# piano-transcription-inference

# Dev / test
pytest
